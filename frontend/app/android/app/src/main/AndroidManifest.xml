<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- 러닝 경로 기록을 위한 위치 권한 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- 화면을 꺼도 러닝 세션 위치가 계속 들어오게 하는 포그라운드 서비스.
         상시 알림이 뜨는 사용자 시작 서비스라 ACCESS_BACKGROUND_LOCATION은
         넣지 않는다(넣으면 Play 심사에서 별도 소명 필요).
         geolocator_android가 <service> 선언을 자기 매니페스트에 갖고 있어
         우리는 권한만 연다. 근거는 docs/background-tracking.md 참고. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <!-- Android 14(API 34)+ 위치형 포그라운드 서비스에 필수 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <application
        android:label="runners_jeju"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- 카카오 네이티브 앱 키. 앱에 담겨 배포되는 값이라 숨기지 않는다 —
             보호는 콘솔에 등록한 패키지명 + 키 해시가 담당한다.

             주의: 이 meta-data는 현재 아무도 읽지 않는 것으로 보인다. 네이티브
             Android SDK 설정 가이드에서 온 잔재이고, kakao_flutter_sdk_common은
             com.kakao.sdk.AppKey 상수를 선언만 해두고 참조하지 않는다. 실제로
             SDK가 쓰는 값은 lib/config/app_config.dart의 kakaoNativeAppKey다.
             로그인 동작을 확인한 뒤 이 블록은 제거해도 된다.

             manifest에서 키가 실제로 쓰이는 곳은 아래 리다이렉트 스킴이다. -->
        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="b996f6761bcc8585602205c0912bc007" />

        <!-- 카카오톡 앱에서 로그인 후 이 앱으로 돌아올 때 쓰는 리다이렉트 액티비티. -->
        <activity
            android:name="com.kakao.sdk.flutter.AuthCodeCustomTabsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:host="oauth" android:scheme="kakaob996f6761bcc8585602205c0912bc007"/>
            </intent-filter>
        </activity>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
